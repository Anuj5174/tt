services:
  - type: web
    name: tigertrace-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 8000
    disk:
      name: tigertrace-data
      mountPath: /app/data
      sizeGB: 1
